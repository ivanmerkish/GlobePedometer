# Project Specification: GlobePedometer (Earth Run)

## 1. Обзор проекта
**GlobePedometer** — это серверное (serverless) многопользовательское веб-приложение, визуализирующее физическую активность пользователей как прогресс движения по экватору Земли.

*   **Концепция:** Пользователи вводят свои шаги, система переводит их в километры и перемещает аватар на восток от начальной точки (Нулевой меридиан / Кения).
*   **Визуализация:** Интерактивный 3D-глобус с отображением всех участников в реальном времени.
*   **Геймификация:** Выбор аватаров, "сонарная" подсветка текущего пользователя, визуализация пройденного пути пунктирной линией.

## 2. Технический стек и Архитектура

### Frontend
*   **Язык:** JavaScript (ES6 Modules).
*   **Сборка:** Native ES Modules (без Webpack/Vite), работает напрямую в браузере.
*   **3D Движок:** `globe.gl` (обертка над Three.js).
*   **Хостинг:** GitHub Pages.

### Backend (BaaS)
*   **Платформа:** Supabase.
*   **База данных:** PostgreSQL.
*   **Auth:** Google OAuth 2.0 (через Supabase Auth).
*   **Безопасность:** RLS (Row Level Security) политики.

### Структура проекта
*   `index.html` — Точка входа, верстка UI, виджет Telegram.
*   `style.css` — Стили интерфейса и модальных окон.
*   `main.js` — Основной контроллер (UI, инициализация, циклы обновлений).
*   `globe.js` — Логика 3D-сцены (инициализация глобуса, рендеринг маркеров и путей, обработка ошибок WebGL).
*   `dbapi.js` — Слой абстракции для работы с Supabase (Auth, CRUD операции).
*   `utils.js` — Утилиты для расчета координат и генерации путей.
*   `config.js` — Конфигурация, ключи API, списки аватаров.
*   `supabase/functions/auth-bridge/` — Edge Function для валидации Telegram и выдачи JWT сессий.

## 3. Реализованный функционал

### 3.1. Авторизация и Профиль
*   **Google SSO:** Нативный вход через Supabase Auth.
*   **Telegram Auth:**
    *   Виджет Telegram на фронтенде.
    *   Backend-валидация хеша через Edge Function (`auth-bridge`).
    *   Автоматическое создание пользователя в `auth.users` (через Admin API).
    *   Выдача полноценной сессии (JWT) для работы RLS политик.
*   **Профиль:**
    *   Статус "Ожидание одобрения" (`is_approved`).
    *   Редактирование никнейма и аватара.
    *   Сохранение состояния сессии.

### 3.2. Визуализация (Глобус)
*   **Движок:** `globe.gl` с обработкой ошибок драйверов (fallback сообщение).
*   **Аватары:** Маркеры с картинками (Люди, Роботы, Животные).
*   **Пути:**
    *   Красные пунктирные линии от старта до текущей позиции.
    *   Защита от мерцания (дедупликация обновлений).
    *   Скрытие путей нулевой длины.
*   **Индикация:** Пульсирующее кольцо ("сонар") вокруг текущего положения пользователя.
*   **Камера:**
    *   Авто-центрирование на пользователе при входе.
    *   Кнопка **"Найти меня" (🎯)** для ручного возврата камеры.
    *   Отсутствие авто-вращения для удобства управления.

## 4. Схема Базы Данных (Supabase)

**Таблица `profiles`**

| Column | Type | Description |
| :--- | :--- | :--- |
| `id` | uuid (PK) | Ссылка на auth.users |
| `email` | text | Email пользователя |
| `nickname` | text | Отображаемое имя |
| `avatar_url` | text | URL иконки |
| `total_steps` | bigint | Сумма шагов (расстояние) |
| `is_approved` | boolean | Флаг доступа (Admin only) |
| `updated_at` | timestamp | Время последнего изменения |

## 5. Roadmap & TODO

### 5.1. UX/UI: Изменение ввода данных
*   [ ] **Read-only Total:** Поле "Общий пробег" сделать нередактируемым (только текст/индикация).
*   [ ] **Add Steps:** Заменить поле ввода "Общий пробег" на "Добавить шаги за сегодня".
*   [ ] **Logic:** Кнопка "Сохранить" должна не перезаписывать `total_steps`, а делать: `new_total = db_total + input_value`.
    *   *Технически:* Реализовать через RPC функцию в Supabase (атомарный инкремент) или транзакцию чтения-записи.

### 5.2. Интерактивность (Mobile/Desktop)
*   [ ] **Улучшенные тултипы:**
    *   *Desktop:* При наведении (Hover) — всплывающая плашка с именем и км .
    *   *Touch:* При нажатии (Tap) на чужой аватар — отображать имя и статистику (возможно, в отдельном блоке UI, чтобы не перекрывалось пальцем).

### 5.3. AI Feature: Загрузка скриншотов (OCR)
*   [ ] **UI Upload:** Кнопка (скрепка/камера) для загрузки скриншота из приложения шагомера (Apple Health, Samsung Health).
*   [ ] **Backend Logic:**
    *   Загрузка изображения в Supabase Storage.
    *   Supabase Edge Function (триггер на загрузку).
*   [ ] **AI Integration:** Отправка картинки в Vision LLM (GPT-4o / Gemini Flash) с промптом: *"Extract the daily step count from this screenshot. Return only the number."*
*   [ ] **Auto-update:** Автоматическое добавление распознанного числа к сумме шагов пользователя.

### 5.4. Технический долг
*   [x] Вынести логику расчета координат (градусы/метры) в отдельный утилитарный класс/модуль (`utils.js`).

### 5.5. Расширение Авторизации
*   [x] **Telegram Auth (Backend):** Развернуть Supabase Edge Function `auth-bridge`.
    *   [x] Установить Supabase CLI.
    *   [x] Команда: `supabase functions deploy auth-bridge --no-verify-jwt`
    *   [x] Секреты: Добавить `TELEGRAM_BOT_TOKEN`.
*   [x] **Telegram Auth (Frontend):**
    *   [x] Добавить Telegram Login Widget в `index.html`.
    *   [x] В `dbapi.js` добавить метод `signInWithTelegram(data)`.
    *   [x] Логика: Виджет -> Получение данных -> Отправка в `auth-bridge` -> Получение токена -> Вход.
*   [ ] Добавить авторизацию через **VK** (ВКонтакте).
*   [ ] Добавить авторизацию через **Yandex**.

### 5.6. Безопасность
*   [ ] **RLS Audit:** Проверить политики безопасности базы данных (Row Level Security). Убедиться, что `ANON_KEY` не дает прав на запись/удаление посторонним.
*   [ ] **Environment Variables:** Проверить, что все приватные ключи (кроме `SUPABASE_URL` и `ANON_KEY`) вынесены в Supabase Secrets.

### 5.7. Инфраструктура
*   [ ] **Миграция хостинга:** Перенести проект с GitHub Pages на платформу с поддержкой Serverless Functions для лучшей безопасности и скрытия бэкенд-логики.
    *   *Кандидаты:* **Vercel** (рекомендуется), **Netlify**, **Cloudflare Pages**.
    *   *Цель:* Скрыть ключи и логику валидации, улучшить скорость доставки (CDN).